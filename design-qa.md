# 设计验收

final result: passed

日期：2026-09-16。采用 Product Design 的视觉方向探索、实现、截图对照流程。用户授权自主选择，并明确要求自然风格、独立布局和 HTML + SVG 作品。

## 视觉依据与实际页面

- source visual truth：`docs/design/selected-reference.png`，森林展廊方向，1487 × 1058 px。它是视觉概念稿，没有可读取的 CSS 尺寸／设备密度。
- implementation：`http://localhost:5173/`，`docs/design/desktop-final.png`。
- 桌面测试 viewport：1440 × 1000 CSS px；浏览器工具实际导出页面内容为 1425 × 990 px。比较以相同宽度下的主要内容区域为准，不把工具截图裁切差异算作布局缺陷，不声称逐像素一致。
- 状态：浅色首页，完整作品数据已加载，演示用户已登录，8 个作品、65 票、剩余 9 票。概念稿为示意数值及未登录状态。
- 手机证据：`docs/design/mobile-final.png`、`mobile-gallery.png`，测试 390 × 844 CSS px，导出 375 × 812 px；另有 320 × 760 边界截图 `mobile-320.png`。

## 全页与局部比较

源稿和最终桌面截图已放在同一次图像输入中共同检查，比较顶部导航、自然风景、提示词卡、侧边浏览区和三列作品卡。保留自然色、衬线标题、陶土色主按钮和柔和卡片；加入实际需要的搜索、排序、复制题目、余票、暂停和静音控件。

有意差异：用户要求作品使用真实 SVG，因此示例鸟及对应封面采用同一份 SVG 源码，不复制概念稿中的位图鸟；匿名评选移除头像／作者姓名。横幅为另一张原创自然风景。控件和卡片留白比概念稿更宽松。概念稿用于方向选择，不是逐像素临摹目标。

局部检查手机卡片截图：标题可换行、模型文字独立一行、票数按钮至少 44px 点击区域。390px 页面 documentElement.scrollWidth=375，小于 viewport 390，无横向溢出。320px 复查按钮、排序和标题不再出现不必要换行。

## 迭代记录

1. 初稿 `desktop-before-qa.png`：侧栏说明过长、辅助字号偏小（P2）。精简侧栏导语并提高正文和元信息字号；`desktop-final.png` 显示更短的导航层级与清晰的作品区域。
2. 手机布局：卡片元信息和票数按钮偏小（P2）。改为 12px 元信息，增加独立行与 44px 点击区域；见 `mobile-gallery.png`。
3. 320px 边界：报名按钮、排序按钮换行（P2）。缩小头部间距、调整字号并隐藏冗余排序图标；`mobile-320.png` 确认已修复。
4. 交互中发现无请求体投票携带 JSON 标头（P0）。请求层修正后浏览器验证 8→9 票、按钮变为“今日已投”、余票 10→9，新增请求层回归测试。
5. 统计数据加载中曾显示假 0，调整为中性加载文案。修改过程发生的 null 读取错误已修正；最终构建和加载后的控制台检查通过。

## 交互证据

- 桌面及手机首页、作品列表已截图检查；详情可打开独立端口的 HTML + SVG 动画预览。
- 实际投票成功、当日重复按钮禁用；榜单第一名 9 票，其余 8 票同为第二名。
- 搜索不存在的名称得到明确空状态；Tab 可从搜索框聚焦清空按钮。
- 报名入口显示固定提示词、HTML 与封面上传；空提交由浏览器定位到作品名称必填项。
- 最终运行后检查控制台：14:27 UTC 之后无新的 error/warn；更早开发热更新中的历史错误不视为最终运行结果。
- 业务闭环与安全限制由 20 项串行自动化测试验证，详见 `docs/verification.md`。

无剩余 P0/P1/P2 设计问题。P3：未来可增加更多不同构图的 SVG 示例。部署容器和真实移动设备上的测试仍未执行，不属于本次已通过的本地视觉验收范围。
