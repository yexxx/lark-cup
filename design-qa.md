# 超级码力视觉与流程验收

final result: passed

日期：2026-09-17。实现运行于本地开发服务，浏览器为 Codex 内置浏览器。样例为显式初始化的本地 SVG 示例数据；验收作品使用独立演示身份，最终已撤回。

## 视觉对照与修复

用户参考为像素场景插画；实现为具有真实文字、导航、按钮与作品流的网站。对照检查侧重色彩、元素风格和连续场景，页面布局按已确认的实施方案组织。

- 已修复 P1：右半边继承旧森林背景，首屏出现两种天空。清理旧飞行场景样式，制作整幅宽屏背景，透明百灵鸟置于统一场景中。
- 已修复 P2：窄侧栏的票数单位换行。额度分母与单位改用较小字号并保持同行。
- 已修复 P2：展区切入榜单的短暂旧数据出现无效名次。为列表模式设置独立组件实例，等待对应响应后显示作品。
- 已修复 P2：手机提示条宽度过窄。提示条按内容计算宽度，同时限制在视口内。
- 桌面／移动首屏、作品列表、榜单、投稿、个人作品、预览与后台已实际打开检查。
- 首页在 1440、768、390、320 宽度检查，documentElement.scrollWidth 与 innerWidth 一致。320 宽度的展区、榜单、详情、投稿、个人页面、后台及设置页同样通过横向溢出检查。
- 桌面和手机标题完整，文字与操作区域清晰；首屏共用连续天空、草地与砖块背景。

修复后证据：`docs/design/super-code-desktop.png`、`docs/design/super-code-mobile.png`、`docs/design/super-code-comparison.png`。对照图已经并列打开检查。未发现遗留 P0/P1/P2 视觉问题。

## 浏览器交互验证

- 演示登录、管理员身份切换、活动设置读取。
- 真实本地封面与 HTML 文件上传、保存草稿、提交审核。
- 管理员驳回并填写原因；个人作品显示“需修改”与反馈；修改后重新提交；撤回后显示“已撤回”。
- 投票成功后卡片票数增加、同作品按钮禁用、余票从 10 变为 9。
- 搜索无匹配作品反馈；榜单验证同票同名次。
- 作品详情打开独立 origin 的 HTML/SVG 交互预览。
- 提示词复制成功反馈；首页鸣叫开关、动画暂停及默认静音状态。
- 浏览器捕获的 error/warn 日志为空。

## 自动化验证与边界

`npm test`：22 项通过，包含新增品牌迁移与活动起止边界测试。现有业务测试覆盖上传限制、草稿／审核／重审／撤回、权限、隔离预览、投票幂等、并发额度耗尽、北京时间换日、关闭窗口、同票排名、分页、搜索及限流。

`npm run build`：TypeScript 检查与生产构建通过。`git diff --check` 通过。

活动未开始／结束由时间边界单测验证；真实年度赛程保持后台原配置。重复投票、额度耗尽、分页与请求拒绝的完整组合由现有业务测试验证；浏览器使用本地样例与演示认证。正式身份接入仍沿用项目原有适配器边界。本次交付为本地项目变更与预览。

## 底部吸顶栏重叠修复

用户反馈并在 1440 × 700 窗口复现：侧栏的 sticky 包含块覆盖整个两行展区，滚动到尾部时票数面板和规则入口压住活动横幅。

为侧栏增加按作品行拉伸的独立边界容器，使 sticky 只在作品行内部移动。复查 1440 × 700 时侧栏底部与横幅顶部间距 18px，768 × 700 时间距 43px；320 × 700 保持普通文档流，横幅完整显示。列表中段吸顶继续正常。生产构建通过。

修复证据：`docs/design/sidebar-bottom-fixed.png`。
